# Tighten mobile spacing and simplify the collection

## Changes
- Reduce the oversized vertical gaps between the process steps, collection area, image slideshow, and following section on small screens.
- Remove the repeated portrait from the Clothing slideshow while keeping it as the opening image.
- Reflow the five category selectors into a compact wrapping layout so every option is immediately visible without horizontal swiping.

## Verification
- Check the page at the current mobile size and on desktop.
- Confirm all five categories remain selectable, the Clothing slideshow no longer contains the portrait, and no marked gap remains excessive.
